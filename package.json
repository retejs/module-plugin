{
  "name": "rete-module-plugin",
  "version": "0.1.5",
  "description": "",
  "main": "build/module-plugin.min.js",
  "scripts": {
    "build": "rete --build rete.config.js",
    "build:dev": "rete --build rete.config.js --watch",
    "prepublishOnly": "npm run build"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "rete": "^1.0.0-alpha.11"
  },
  "devDependencies": {
    "rete-cli": "^0.2.0"
  }
}
